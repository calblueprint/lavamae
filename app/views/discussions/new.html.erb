<section class="create">
  <div class="container signup-container">
    <div class="signup-row">
      <h3>Create a Discussion</h3>
      <%= form_for @discussion, url: discussions_path do |f| %>
        <!-- <head> New Discussion Form</head> -->
      <div class="input-field">
        <%= f.label :title %>
        <br>
        <%= f.text_field :title %>
      </div>

      <div class="input-field">
        <p>
          <%= f.label :body %>
          <br>
          <%= f.text_area :content %>
        </p>
      </div>

      <div class="discussion-tag-container" id="tags">
        <i class="fa fa-tags fa-lg"></i>
        <% @all_tags.each do |t|%>
        <button type="button" class="discussion-tag" name=<%= t %>><%= t %></button>
        <% end %>
      </div>
      <% @all_tags.each do |t|%>
        <%= check_box_tag("tags[]", t, false, {class: "hidden", id: t}) %>
      <% end %>
      <br />
      <br />
      <%= f.submit 'create', class:"btn btn-blue" %>
      <% end %>
    </div>
  </div>
  <script>
    $('.discussion-tag').click(function(){
      $(this).toggleClass('checked');
      var cb = document.getElementById(this.name);
      cb.checked = !cb.checked;
    });
  </script>
</section>
