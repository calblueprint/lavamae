<% if !current_user || !current_user.is_admin %>
	<!-- You do not have access to this page. -->
<% end %>

<%if current_user &&  current_user.is_admin %>
	<%= react_component('PendingAdminUsers', pending_users: @pending_admins, user_id: current_user.id) %>
<% end %>